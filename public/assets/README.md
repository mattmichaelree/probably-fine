# Production assets

`concept/` contains references. Add production assets under `characters/`, `locations/`, `food/`, `items/`, `ui/`, and `audio/` as the game is built. Record provenance and license in `docs/ASSET_NOTES.md`.
